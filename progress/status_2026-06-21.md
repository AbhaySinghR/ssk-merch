# SSK Merch — Status: 2026-06-21

## Done today

### Sign-in / alumni registration page (`/sign-in`)
- Built registration form: Title, First Name, Last Name, Email (regex-validated), Phone (country code + flag picker via `react-phone-number-input`, validated with `libphonenumber-js`), School Number, Batch.
- Server Action (`src/app/sign-in/actions.ts`) validates fields, saves submissions to `data/registrations.json` (gitignored — contains PII), and sends a confirmation email via Resend.
- Email sending degrades gracefully when `RESEND_API_KEY` isn't set (saves registration, skips email, no crash) — found and fixed a real bug here where a missing key used to crash the whole form.
- Navbar "SIGN IN" now links to `/sign-in` (was a dead `#` link).

### Branding
- Replaced all "SSK" text with "Sainik School Kapurthala" across navbar, page titles, footer, sign-in copy, and confirmation email.
- Added the school crest image at `public/images/branding/crest.png` (transparent background, smooth anti-aliased edge) and placed it on the right side of the Hero ("Wear the Legacy") section in a two-column layout.
- Created `public/images/gallery/` for future use.

### Homepage content
- Softened Heritage section copy to feel inclusive rather than purely defence-focused (still 3-column "I / II / III" structure: School History, Our Mission, Alumni Community).
- Added new "Why This Merch Exists" section ("More Than Merchandise") with inclusive messaging, positioned after Heritage.
- Closing quote *"Saikapians Are Second To None"* lives inside the "Why This Merch Exists" section (not a separate section).
- Removed "READ MORE" links from the Heritage I/II/III columns and fixed a misalignment bug (stray `first:pt-10` class was pushing column I down on desktop only).

### Placeholder pages
- Added themed "Coming Soon" pages at `/shop`, `/gallery`, `/community`, `/track-order` (shared `ComingSoonPage` component).
- Wired up navbar links (desktop + mobile) and Hero's "SHOP NOW" button to these real routes instead of `#`.

### Community page (`/community`) — built from `Plan/Community.md`
- Replaced the `/community` placeholder with a real 3-section page:
  - `CommunityHero.tsx` — "Saikapian Community" heading/subheading/intro + banner placeholder.
  - `ChaptersSection.tsx` — "Our Chapters" with India / United States / Canada cards (copy from the plan), each with a photo placeholder.
  - `CommunityGallery.tsx` — "Community Moments" 6-tile placeholder gallery.
- Added a shared `PhotoPlaceholder.tsx` (icon + label) used by both the chapter cards and the gallery grid, so real photos can later be swapped in via `next/image` without touching layout code.

## Known follow-ups / not done yet
- **Email delivery**: `RESEND_API_KEY` is not yet configured in `.env.local` — registrations currently save but don't send a real email. Need a Resend account + API key, and ideally a verified sending domain.
- **Registration storage**: currently a flat JSON file (`data/registrations.json`). Explicitly deferred — may want a real database later.
- **"OUR STORY" button** on Hero still points to `#` — not yet wired to a page/anchor.
- **Gallery images**: `public/images/gallery/` folder exists but is empty — no gallery content/page built yet (the `/gallery` route is still a "Coming Soon" placeholder). The new Community page's placeholder tiles are also waiting on real photos (India/US/Canada chapter photos, reunion/event photos, etc.).
- Heritage section copy was softened but could use another pass if more inclusive language is wanted.

## Notes for next session
- Dev server caching (Turbopack) was flaky a couple of times this session — if styles/images seem stale or wrong after edits, try restarting `npm run dev` before debugging further.
- Crest image source lives at `public/images/branding/crest.png` — already has transparent background and clean edges, ready to reuse elsewhere if needed.
